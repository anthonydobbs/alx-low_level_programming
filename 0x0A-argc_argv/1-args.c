#include <stdio.h>
#include "main.h"

/**
 * main - prints the number of arguments passed
 * @argc: argument counts
 *
 * Return: 0 Always
 */
int main(int argc)
{
		printf("argc = %d\n", argc);

	return (0);
}
